"""Auth models package

This module provides minimal model stubs used across the codebase for typing
and lightweight runtime needs. The real implementation can replace these
definitions without changing import paths.
"""

from .user_models import User, PasswordResetToken  # noqa: F401

