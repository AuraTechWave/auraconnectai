fastapi
uvicorn
sqlalchemy
psycopg2-binary
alembic
pydantic>=2.0
pydantic-settings
python-multipart
boto3
mkdocs
mkdocs-material
fastapi-limiter
redis
httpx
passlib[bcrypt]
python-jose[cryptography]
argon2-cffi
email-validator
jinja2
aiofiles
jsonschema

# Payment Gateway SDKs (only stripe is used directly in code)
stripe>=5.0.0

# Payment Support Libraries
arq>=0.25.0  # Redis queue for webhook processing
prometheus-client>=0.16.0  # Metrics collection
croniter
apscheduler

# Note:
# - Removed unused/unresolved SDKs (squareup, paypalrestsdk) to prevent CI pip failures.
#   If needed later, add them to a separate optional requirements file and gate imports.
